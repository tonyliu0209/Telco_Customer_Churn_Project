# 資料處理
pandas>=2.0.0
# 為了相容 SHAP，暫時避開 numpy 2.x
numpy>=1.26.0,<2.0.0

# 模型相關 (必須與訓練環境一致)
scikit-learn==1.8.0
xgboost==3.2.0
joblib>=1.5.0

# 網頁與視覺化
# 建議用新版，支援更多現代元件
streamlit>=1.40.0
matplotlib>=3.8.0
lime
scipy
